\documentclass{ctexart}
\title{snake for practicing git skills}
\author{woo}

\begin{document}
\maketitle{}
\section{Introduction}
It's a simple snake game and its rule just like others.
The first version is finished by myself, and the following updates is completed by AI.
\section{AI Key Words}
\begin{itemize}
    \item "如何实现按键响应" and "你没有把终端设置改回去把" for the feature of without erntering erntering
    \item "把输出改成原地刷新（使用 ncurses 或 ANSI 清屏），避免大量滚屏" for outputing timely
    \item "增加蛇碰到自己后死亡的机制" for fixing the bug that the snake won't die when touching itself
    \item "修复蛇能立刻反向的bug" 
\end{itemize}
\section{Git Log}
d82a027 (HEAD -> code, origin/code) fix: fix the bug snake can take a 180 dgree turn\\
f565bfb fix:fix the bug snake won't die if touching itself\\
7e4863e refacter: use macro to define constant value\\
f14a35d feat:refresh the output instead of rolling The previous version keeps outputing. Now use ANSI to refresh the terminal.\\
aff7c65 feat: enable user to play without entering enter The previous version need player to type enter before the programme can output.Reset the terminal sttings allows the current version to output stimely.\\
0a772ce initial\\
fa8bb52 (origin/main, main) initial\\
\end{document}
